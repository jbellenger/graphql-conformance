[package]
name = "conformer"
version = "0.1.0"
edition = "2024"

[dependencies]
async-graphql = { path = "./build", features = ["dynamic-schema"] }
tokio = { version = "1", features = ["rt", "macros"] }
serde_json = "1"
